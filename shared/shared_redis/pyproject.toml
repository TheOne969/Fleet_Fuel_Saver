[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shared_redis"
version = "0.1.0"
description = "Shared Redis connection pool for microservices"
dependencies = [
    "redis",
]

